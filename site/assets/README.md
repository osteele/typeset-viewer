# Landing-page images

Drop screenshots here and reference them from `../index.html`.

- `screenshot-main.png` — a document rendered in Typeset Viewer, for the hero (roughly 16:10, retina width ~2400px). The hero currently shows a placeholder until this exists; swap the placeholder `<div>` in `index.html` for `<img src="assets/screenshot-main.png" alt="A document rendered in Typeset Viewer">`.
- Optional additional feature shots (notes sidebar, review panel, presentation mode).
